# (C) 2023 GoodData Corporation
version: 3

project:
  id: gooddata-python-sdk

telemetry:
  scope: 'off'

# Per-package paths.only is injected at scan time by .github/workflows/fossa.yaml
# so each package's deps land under its own fossa_gd_* branch in the FOSSA UI.
# This anchor file exists for local `fossa analyze` runs and to satisfy
# fossa-action's expectation of a config file in the repo root.
